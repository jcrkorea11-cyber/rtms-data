# ─────────────────────────────────────────────────────────────
# 이 저장소는 PUBLIC 입니다. 아래 항목은 절대 커밋되면 안 됩니다.
#   - 서버 IP / 도메인
#   - Reality 개인키, UUID, Hysteria2 비밀번호
#   - WireGuard 개인키
#   - 완성된 클라이언트 설정(위 정보가 전부 들어있음)
# 스크립트와 템플릿만 커밋하고, 실제 값은 서버에서 생성해 로컬에만 둡니다.
# ─────────────────────────────────────────────────────────────

out/
*.env
node-*.env
*.conf
*.key
*.crt
*.pem
*.png
links.txt
sing-box-*.json
